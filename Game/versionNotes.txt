Current Game Version
	v.0.2.897


 ** Last Update Note:
 		PATH FINDING:
			Working on the timing of when the slime switches to the next box for movement. Currently the initial position of the slime isn't
			saved to the path list. So I need to artificially push the slime to the next box to have it move. Right now the Find_mySquare function
			is based around the center of the slime. That point may need to be adapted to be placed differently based on its next moves.
			All because the slime can get clipped on edges due to not moveing far enough before picking its next direction. 
